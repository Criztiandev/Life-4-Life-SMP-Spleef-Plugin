package dev.criztian.spleef.game;

public enum ParticipantStatus {
    /** In the game and still standing. */
    ALIVE,
    /** Out for this round — spectating until the next /spleef round. */
    ELIMINATED
}
